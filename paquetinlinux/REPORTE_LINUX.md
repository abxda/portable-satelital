# REPORTE — build Linux x64 del Laboratorio Satelital Portable

> Llena TODOS los campos. "No aplica" o "falló: <error>" también son respuestas válidas.

## Entorno
- Fecha/hora:
- Ubuntu (lsb_release -ds):
- Arquitectura (uname -m):
- Espacio libre al inicio:
- Commit del repo (git log --oneline -1):

## FASE 2 — Portable
- ¿Build exitoso? (sí/no):
- **sha256 del tarball** (el dato MÁS importante):
- Tamaño del tarball (bytes):
- Versión de Python del portable:
- Lock usado: (requirements.lock original / requirements-linux.lock regenerado)
- Salida COMPLETA de la "PRUEBA DE RELOCALIZACIÓN" (pegar):

```
(pegar aquí)
```

## FASE 3 — Prueba como alumno
- jupyterlab --version:
- pip --version tras extraer en /tmp:
- ¿`00_Bienvenida_ejecutado_linux.ipynb` se generó sin errores? (sí/no):
- Prueba HTTP de Jupyter (código devuelto, si la hiciste):

## FASE 4 — Launcher SatLab Linux (opcional)
- ¿Compiló? (sí/no/no intentado):
- Comando usado (con o sin -tags webkit2_41):
- sha256 del binario SatLab:
- Resultado de `--headless-smoke` (pegar satlab-headless.log):

```
(pegar aquí)
```

- ¿GUI probada? ¿`satlab.log` dice "UI LISTA"? (sí/no/sin escritorio):

## Entregables
- Ruta de la carpeta `entregables/`:
- Contenido de `SHA256SUMS_entregables.txt` (pegar):

```
(pegar aquí)
```

- ¿Subiste el tarball a HF? (solo si el Dr. lo pidió) — oid LFS reportado:

## Problemas, desviaciones y notas para el líder
-
